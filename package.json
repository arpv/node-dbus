{
  "name": "node-dbus",
  "description": "A minimal node based wrapper over libdbus",
  "version": "0.1.0",
  "engines": ["node >= 0.5.1"],
  "scripts": {
    "install": "node-waf configure build install"
  },
  "main": "dbus.js"
}
